using CoreProgram = Edda.Const.Program;

namespace Edda.Avalonia;

internal static class Program {
    public static int Main(string[] args) {
        Console.WriteLine($"{CoreProgram.Name} Avalonia UI scaffold is ready.");
        Console.WriteLine("TODO: add Avalonia packages and wire AppBuilder/MainWindow shell.");
        return 0;
    }
}